export const STORAGE_NAME = 'DEMO';
export const AUTH_TOKEN = `${STORAGE_NAME}.auth_token`;
export const ERROR_LOG = `${STORAGE_NAME}.errors`;

export const DEFAULT_PAGINATION_PAGE_SIZE = 8;
export const DEFAULT_AUTO_LOAD_PAGE_SIZE = 100;
